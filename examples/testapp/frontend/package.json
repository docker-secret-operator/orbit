{
  "name": "docker-rollout-testapp-frontend",
  "version": "1.0.0",
  "description": "docker-rollout test frontend — Express + HTML UI calling the backend API",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
