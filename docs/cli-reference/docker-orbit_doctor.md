## docker-orbit doctor

Run a comprehensive health check of the Orbit installation

### Synopsis

Doctor verifies the pieces Orbit depends on: the Docker Engine, the
compose file, the running proxy (if any), state directory permissions, and
this binary's plugin installation. Every check reports PASS, WARNING, or
ERROR with a concrete next step for anything that isn't PASS — never a raw
stack trace.

Example:
  docker orbit doctor
  docker orbit doctor --json
  docker orbit doctor --control-addr http://localhost:9901
  docker orbit doctor --file docker-compose.prod.yml

```
docker-orbit doctor [flags]
```

### Options

```
      --control-addr string   Proxy control API address (default "http://localhost:9900")
      --file string           Compose file to validate (default "docker-compose.yml")
  -h, --help                  help for doctor
      --json                  Output as JSON
      --project string        Service/project name (default: $ORBIT_PROXY_INSTANCE, else "default")
```

### SEE ALSO

* [docker-orbit](docker-orbit.md)	 - Zero-downtime deployments for Docker Compose

###### Auto generated by spf13/cobra on 3-Jul-2026
